version: 1
frontend:
  phases:
    preBuild:
      commands:
        - env | cut -d= -f1 | sort
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
